% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_taylor_demo.R
\name{simulate_taylor_demo}
\alias{simulate_taylor_demo}
\title{Simulate demo confounding paths for Taylor panel figures}
\usage{
simulate_taylor_demo(
  delta_max = 1.5,
  step = 0.02,
  theta0 = 0.4,
  slope = -0.7,
  kappa = 0.4
)
}
\arguments{
\item{delta_max}{Single positive numeric scalar. Upper bound of the
\eqn{\delta} grid.}

\item{step}{Single positive numeric scalar. Step size for the grid.}

\item{theta0}{Single numeric scalar. Baseline effect at \eqn{\delta = 0}.}

\item{slope}{Single numeric scalar. First-order slope at \eqn{\delta = 0}.}

\item{kappa}{Single non-negative numeric scalar. Curvature magnitude.}
}
\value{
A named list with elements \code{linear}, \code{concave}, and \code{convex}, each
a \code{\link[=new_confoundsens]{new_confoundsens()}} object.
}
\description{
Generates three synthetic sensitivity paths — linear, concave-down, and
convex-up — sharing the same baseline effect \eqn{\theta(0)} and first-order
slope, but differing in second-order curvature. These toy paths illustrate
the difference between tangent-based (first-order) and local quadratic
(second-order) sensitivity approximations.
}
\details{
The three regimes are:
\itemize{
\item \strong{Linear}: \eqn{\theta(\delta) = \theta_0 + s\delta}
\item \strong{Concave-down}: \eqn{\theta(\delta) = \theta_0 + s\delta - \kappa\delta^2}
\item \strong{Convex-up}: \eqn{\theta(\delta) = \theta_0 + s\delta + \kappa\delta^2}
}
}
\examples{
sims <- simulate_taylor_demo(
  delta_max = 1, step = 0.05, theta0 = 0.4, slope = -0.7, kappa = 0.4
)
sims$linear
plot_robustness_curve(sims$concave)
}
