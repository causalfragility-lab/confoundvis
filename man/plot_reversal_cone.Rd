% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_reversal_cone.R
\name{plot_reversal_cone}
\alias{plot_reversal_cone}
\title{Reversal cone geometry plot}
\usage{
plot_reversal_cone(
  theta0 = 0.4,
  delta = 0.2,
  q_range = c(0, 1),
  p_range = c(-1, 1),
  grid_n = 200L,
  show_boundary = TRUE,
  show_volume = TRUE
)
}
\arguments{
\item{theta0}{Numeric; baseline estimated effect at zero confounding (must
be nonzero).}

\item{delta}{Numeric; the fixed confounding effect magnitude \eqn{|\delta|}
at which the cross-section is evaluated (> 0).}

\item{q_range}{Numeric vector of length 2; range of the confounding
prevalence parameter \eqn{q \in [0, 1]} (default \code{c(0, 1)}).}

\item{p_range}{Numeric vector of length 2; range of the confounding
impact parameter \eqn{p} (default \code{c(-1, 1)}).}

\item{grid_n}{Integer; grid resolution for each axis (default 200).}

\item{show_boundary}{Logical; draw the reversal boundary curve.}

\item{show_volume}{Logical; annotate with the cone volume proportion.}
}
\value{
A ggplot object.
}
\description{
Visualizes the two-dimensional cross-section of the reversal cone
\eqn{C_l} at a fixed confounding effect magnitude \eqn{|\delta|}. The cone
partitions the \eqn{(q, p)} confounding parameter space into an
\strong{attenuation zone} (where the effect shrinks but does not reverse sign)
and a \strong{reversal zone} (where the sign changes). The boundary between
zones is the reversal curve derived from the multilevel mITCV framework.
}
\examples{
plot_reversal_cone(theta0 = 0.40, delta = 0.20)
plot_reversal_cone(theta0 = 0.40, delta = 0.50)
}
