% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_reversal_cone.R
\name{plot_cone_comparison}
\alias{plot_cone_comparison}
\title{Compare reversal cones across multilevel components}
\usage{
plot_cone_comparison(
  theta0_within = 0.35,
  theta0_between = 0.5,
  delta_within = 0.2,
  delta_between = 0.3,
  ...
)
}
\arguments{
\item{theta0_within}{Numeric; within-cluster baseline effect.}

\item{theta0_between}{Numeric; between-cluster baseline effect.}

\item{delta_within}{Numeric; within-cluster confounding magnitude (> 0).}

\item{delta_between}{Numeric; between-cluster confounding magnitude (> 0).}

\item{...}{Additional arguments passed to \code{\link[=plot_reversal_cone]{plot_reversal_cone()}}.}
}
\value{
Invisibly, a list with ggplot objects \code{within} and \code{between}.
The plots are drawn side-by-side to the current device.
}
\description{
Produces a two-panel plot comparing the reversal cone cross-sections for
the within-cluster and between-cluster confounding components in multilevel
settings. Each panel calls \code{\link[=plot_reversal_cone]{plot_reversal_cone()}} and they are displayed
side by side using faceting.
}
\examples{
plot_cone_comparison(
  theta0_within  = 0.35, theta0_between = 0.50,
  delta_within   = 0.20, delta_between  = 0.30
)
}
